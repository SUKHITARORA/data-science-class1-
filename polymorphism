class cricket:
    def __init__(self,player,score):
        self.__player=player
        self.__score=score
    def info(self):
        print(f"if cricket - player: {self.__player},score: {self.__score}")
    def play(self):
        print(f"{self.__player} Hits a 6")
    def get_score(self):
        return self.__score
    def set_score(self,newscore):
        if newscore >=0:
            self.__score= newscore
            print(f"score updated to{self.__score}")
        else:
            print("score can not be negetive")
class football:
    def __init__(self,player,score):
        self.__player=player
        self.__score=score
    def info(self):
        print(f"if football - player:{self.__player},score:{self.__score}")
    def play(self):
        print(f"{self.__player}scores a goal")
    def get_score(self):
        return self.__score
    def set_score(self,newscore):
        if newscore>=0:
            self.__score=newscore
            print(f"score updated to {self.__score}")
        else:
            print("score can not be negetive")
object=cricket("sukhit",85)
object2=football("sukhit",3)
print("sports scoreboard")
for i in (object,object2):
    i.info()
    i.play()
    print()
print("direct change attempt")
object.__score=300
print(f"current score is {object.get_score()}")
print("updated score")
object.set_score(400)
print(f"current score is {object.get_score()}")
